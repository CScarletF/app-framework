DB_HOST=192.168.20.190
# Port 5000, not 5432 -- this is the Keepalived VIP's HAProxy "master"
# listener (routes to whichever node is currently Patroni primary), not
# a direct Postgres connection.
DB_PORT=5000
DB_NAME=webapp_demo
DB_USER=webapp_app
DB_PASSWORD=
APP_ENV=production
APP_DEBUG=false
